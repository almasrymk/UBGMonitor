<UserControl x:Class="ClientAgent.UI.Views.SimpleTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorder}" Margin="10">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding TitleText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding BodyText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="#AAAAAA" Margin="0,12,0,0" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="640"/>
        </StackPanel>
    </Border>
</UserControl>
